<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Free Market - Mini Message Board Project</title>
    <link rel="stylesheet" href="/styles.css">
</head>
<body>
    <div class="main">
        <div class="mainInner">
            <h1><%= title %></h1>
            <% messages.forEach((message) => { %>
                <div class="message">
                    <p><%= message.text %></p>
                    <p><%= message.user %></p>
                    <p><%= message.added %></p>
                </div>
            <% }) %>
        </div>
    </div>
</body>
</html>
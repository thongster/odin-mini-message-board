<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Maya's House - Mini Message Board Project</title>
    <link rel="stylesheet" href="/styles.css">
    <link rel="icon" type="image/x-icon" href="/mushicon.png">
</head>
<body>
    <div class="main">
        <div class="mainInner">
            <div class="header">
                <h1><%= title %></h1>
                <a href="/new" class="newMsgBtn">+New</a>
            </div>
            <% messages.forEach((message) => { %>
                <div class="message">
                    <p><%= message.text %></p>
                    <p><%= message.username %></p>
                    <p>
                        <%= message.added.toDateString() %>,
                        <%= message.added.toLocaleTimeString() %>
                    </p>
                    <div class="action">
                        <a href="/messages/<%= message.id %>" class="openBtn">Open</a>
                    </div>
                </div>
            <% }) %>
        </div>
    </div>
</body>
</html>